/**
 * El lienzo 3D en el móvil: el `Canvas` de fibra sobre `expo-gl`.
 *
 * Gemelo nativo de `Lienzo.tsx`. Metro lo elige solo al compilar para iOS o
 * Android por la convención `.native`.
 */
export { Canvas } from '@react-three/fiber/native';
